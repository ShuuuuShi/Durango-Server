# Licensing Notice / ข้อความเรื่องลิขสิทธิ์

**[ไทย](#ไทย) · [English](#english) · [中文](#中文)**

---

## ไทย

License ของโปรเจกต์นี้ (**GPL-3.0**) ครอบคลุม **เฉพาะโค้ดที่เขียนขึ้นใหม่** ในโปรเจกต์นี้เท่านั้น ได้แก่:

- `server/` — ตัวเซิร์ฟเวอร์ที่เขียนใหม่ทั้งหมดด้วย C#/.NET 9
- `test-client/` — ตัวเกมเทส
- `mod-sdk/`, `client-mod-sdk/`, `tools/MemoryBotMod/` — ระบบม็อด
- `docs/` — คู่มือทั้งหมด

**ส่วนที่ไม่อยู่ภายใต้ license นี้:**

สิ่งที่ derive จากตัวเกม *Durango: Wild Lands* — รวมถึงแต่ไม่จำกัดเพียง `server/GameCode/` (โค้ดที่ยกมาจากตัวเกม), โปรโตคอลเครือข่าย, ชื่อไอเทม/สกิล/สัตว์, ข้อมูลตารางต่าง ๆ ใน `server/data/assets/` และข้อมูลเทอร์เรน — เป็น**ทรัพย์สินทางปัญญาของ NEXON** ไม่ได้ถูกปล่อยภายใต้ GPL-3.0 ห้ามใช้เชิงพาณิชย์ และโปรเจกต์นี้ไม่มีส่วนเกี่ยวข้องกับ NEXON แต่อย่างใด

## English

This project's license (**GPL-3.0**) covers **only code newly written** for this project, namely:

- `server/` — the from-scratch C#/.NET 9 server
- `test-client/` — the console test client
- `mod-sdk/`, `client-mod-sdk/`, `tools/MemoryBotMod/` — the modding system
- `docs/` — all guides

**What is NOT covered by this license:**

Anything derived from the game *Durango: Wild Lands* — including but not limited to `server/GameCode/` (code lifted from the game), the network protocol, item/skill/animal names, the data tables in `server/data/assets/`, and terrain data — is the **intellectual property of NEXON**. It is not released under GPL-3.0, must not be used commercially, and this project is not affiliated with NEXON in any way.

## 中文

本项目的许可证（**GPL-3.0**）仅覆盖**为本项目新编写的代码**，包括：

- `server/` — 从零编写的 C#/.NET 9 服务器
- `test-client/` — 控制台测试客户端
- `mod-sdk/`、`client-mod-sdk/`、`tools/MemoryBotMod/` — 模组系统
- `docs/` — 全部指南

**本许可证不覆盖的内容：**

任何从游戏 *Durango: Wild Lands* 衍生的内容 —— 包括但不限于 `server/GameCode/`（来自游戏本体的代码）、网络协议、物品/技能/动物名称、`server/data/assets/` 中的数据表以及地形数据 —— 均为 **NEXON 的知识产权**，不以 GPL-3.0 发布，不得用于商业用途。本项目与 NEXON 无任何关联。
