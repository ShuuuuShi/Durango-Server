# namespace `NestedPrefab`

> auto-generated จากซอร์สจริง — ลายเซ็นและเลขบรรทัดตรงกับไฟล์ เปิดตามได้ทันที

1 ไฟล์

## `NestedPrefab/NestedPrefabLinker.cs`

258 บรรทัด

**class `NestedPrefabLinker`** — บรรทัด 10–12

**class `NestedPrefabLinker`** — บรรทัด 14–257

| บรรทัด | สมาชิก | หมายเหตุ |
|---:|---|---|
| 52 | `private void InitializeLink()` |  |
| 79 | `protected virtual void Awake()` | Unity lifecycle |
| 84 | `protected virtual void OnEnable()` | Unity lifecycle |
| 92 | `protected virtual void OnDisable()` | Unity lifecycle |
| 100 | `private void OnValidate()` |  |
| 109 | `protected virtual void Link()` |  |
| 199 | `protected virtual void OnLinked()` |  |
| 203 | `protected virtual void Unlink()` |  |

---
